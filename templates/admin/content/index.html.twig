{% extends "admin/layout.html.twig" %}

{% trans_default_domain "admin" %}

{% block main_body %}
    <turbo-frame id="table_crud" class="flex flex-col gap-4" data-turbo-action="advance">
        <h1 class="text-center text-4xl">
            {{ "page.content.title"|trans }}
        </h1>

        <div class="flex min-h-[28.5rem] gap-2 flex-col items-start md:flex-row">
            {{ knp_menu_render('back_office', { template: knp_custom_menu }) }}
            {{ component('table', { config }) }}
            {{ component(
                "button",
                {
                    ariaLabel: ("icon.add_circle")|trans,
                    centered: false,
                    "data-turbo-frame": "_top",
                    href: path("admin_content_new"),
                    icon: "add-circle",
                    square: true,
                })
            }}
        </div>

        <div class="flex items-center justify-center">
            {{ knp_pagination_render(config.pagination, 'knp_custom/paginator.html.twig') }}
        </div>
    </turbo-frame>
{% endblock %}
