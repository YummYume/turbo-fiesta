{% trans_default_domain "admin" %}

{{ form_start(form) }}
{{ form_row(form.title) }}
{{ form_row(form.blocks) }}
{{ form_row(form.published) }}
{{ form_row(form.categories) }}

{% if editing %}
    {{
        component(
            "button",
            {
                class: "mb-4 disabled:cursor-not-allowed",
                centered: false,
                content: "page.content_edit.send_notification_email"|trans,
                disabled: notificationEmailDisabled ? false : true,
                form: "content-send-notification-email-form",
                submit: true,
                variant: "info",
            }
        )
    }}
{% endif %}

{{ component("button", { content: button_label|default("common.save"|trans), submit: true, centered: true }) }}
{{ form_end(form) }}
